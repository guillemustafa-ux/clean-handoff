PORT=3000

# Secret configured as the Vapi assistant's server URL credential.
# Must match X-Vapi-Secret header (or the equivalent auth Vapi sends) on every
# request to /vapi/tool-calls and /vapi/events. Generate any random string.
VAPI_SERVER_SECRET=change-me
